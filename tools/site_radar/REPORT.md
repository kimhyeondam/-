# 현장 레이더 — 구현 보고

## 상태

파이프라인은 완성해서 검증까지 마쳤다. **API 스펙 연결만 남았다.**

이 작업 환경에서 `*.go.kr` 도메인 전체가 네트워크 정책으로 차단되어 있다
(`www.data.go.kr`, `apis.data.go.kr`, `data.gg.go.kr` 모두 CONNECT 터널 403).
따라서 공식 문서를 열어 엔드포인트·파라미터명·응답 항목명을 확인할 수 없었다.

"확인되지 않은 파라미터명·필드명은 추측해서 쓰지 않는다"는 원칙에 따라,
**API 스펙을 코드에서 완전히 분리해 `api_spec.yaml` 로 빼냈다.**
코드에는 나라장터 항목명이 단 하나도 하드코딩되어 있지 않다.

## 미확인 — 문서에서 직접 옮겨 적어야 하는 항목

`api_spec.yaml` 의 `TODO` 21개가 그대로 목록이다. `python tools/site_radar/run.py` 를
실행하면 채워야 할 항목을 전부 출력하고 exit code 2 로 멈춘다.

확인해야 할 문서:

| 서비스 | 문서 |
|---|---|
| 조달청_나라장터 입찰공고정보서비스 | https://www.data.go.kr/data/15129394/openapi.do |
| 조달청_나라장터 낙찰정보서비스 | https://www.data.go.kr/data/15129397/openapi.do |

두 URL은 검색 결과로 확인된 실제 데이터셋 페이지다. 각 페이지의
"요청 변수" / "출력 결과" 표에 적힌 이름을 그대로 옮기면 된다.

특히 확인이 필요한 것:

1. **엔드포인트 base URL과 공사 오퍼레이션명** — 나라장터 API는 개편 이력이 있어
   기억이나 블로그 글에 의존하면 안 된다. 문서의 "서비스 URL"을 그대로 쓸 것.
2. **지역 필터 수단** — 요청 파라미터로 지역을 걸 수 있는지, 아니면 응답을 받아서
   걸러야 하는지. 현재 구현은 **응답을 받아서 거르는 쪽**을 가정한다
   (발주처명·공고명에서 시·군명을 찾는다). 요청 단계 필터가 있으면 호출량이 줄어드니
   `bid.extra_params` 에 추가하면 된다.
3. **조회기간 상한** — 문서에 상한이 있으면 `bid.max_range_days` 를 그 값으로 바꿀 것.
4. **금액·개찰일시·상세URL 항목의 존재 여부** — 목록 오퍼레이션 응답에 없으면
   `fields` 에서 `null` 로 두면 된다. 그 열은 공란으로 나가고, 금액 필터는 자동으로 건너뛴다.

## 커버 범위 불확실 — 공기업 발주

LH·한국도로공사·한국수자원공사 발주가 나라장터에 얼마나 올라오는지,
각 기관 자체 전자조달 시스템에만 올라오는 공고가 있는지 **문서로 확인하지 못했다**
(위와 같은 차단 사유). 현재 구현은 나라장터 응답의 수요기관명에
`agency_keywords` 를 걸어 잡는 방식이고, 자체 시스템 전용 공고는 잡지 못한다.

첫 실전 실행 뒤, 알고 있는 해당 기관 발주 건이 시트에 나오는지 대조해서
누락이 크면 별도 소스를 붙여야 한다.

## 설계 판단 두 가지 (알려둘 것)

**1. 중복 판정 색인을 별도 파일로 뒀다.**
시트 열 구성이 12개로 고정이라 공고번호를 넣을 자리가 없다. 그래서 공고번호는
`data/_seen_bids.json` 에 따로 쌓는다. 이 파일이 지워져도 기존 CSV의
(현장명, 발주처, 공고일) 조합으로 대체 판정하므로 중복이 터지지는 않는다.

**2. 시·군 축약형을 함께 잡는다.**
검증 중에 실제로 걸린 문제다. 지자체 공고는 "김해시 진영읍…"이라 그대로 잡히는데,
LH 공고는 "김해 진례지구…"라 `김해시` 로는 안 걸린다. 그래서 접미사(시/군/구)를
뗀 축약형도 매칭하되, 뒤에 한글이 바로 붙으면 제외한다
(`고성군` → `고성` 이 `고성능` 에 걸리는 것을 막는다).

## 검증 결과

API 스펙이 없어 **실제 API 호출은 하지 못했다.** 나머지는 픽스처
(`fixtures/sample.json`, 원본 7건)로 실행해 확인했다.

| 항목 | 결과 |
|---|---|
| 스펙 미완성 시 추측 없이 중단 | 통과 — 미기입 21개 항목을 출력하고 exit 2 |
| 필터 동작 | 통과 — 7건 중 3건 통과, 4건 제외 |
| 제외 사유별 확인 | 지역 밖(부산) / 제외어(전기) / 금액 미달(5천만) / 발주처 미해당(민간) 각 1건 |
| 품목 추정 규칙 | 통과 — 하수도→흄관·맨홀, 도로확포장→경계블록·측구·집수정, 하천→옹벽블록·호안블록 |
| 중복 미증가 | 통과 — 같은 명령 2회 실행 후 신규 0건, 파일 3행 유지 |
| 대상 지역 밖 행 | 통과 — 0행 |
| 엑셀 호환 | 통과 — UTF-8 BOM (`ef bb bf`) 확인 |
| 축약형 오탐 방지 | 통과 — `고성능 펌프장` 은 `고성군` 으로 잡히지 않음 |

**실제 API 호출과 공고URL 대조(검증 2번)는 하지 못했다.** 스펙과 인증키가
채워진 환경에서 실행해야 한다.

## 다음 단계

1. 공공데이터포털에서 두 서비스 활용신청 → `export G2B_SERVICE_KEY=...`
2. 두 문서 페이지를 열어 `api_spec.yaml` 의 TODO 21개를 채운다
3. `python tools/site_radar/run.py --regions 김해시 창원시 --lookback-days 7`
4. 나온 CSV의 첫 3행을 공고URL과 눈으로 대조한다
5. 맞으면 cron 등록: `0 8 * * 1 cd /경로 && python tools/site_radar/run.py`
