# 현담토목 운영 OS — Claude 작업 지침

혼자 운영하는 콘크리트 2차제품 제조사입니다. 사장이 곧 영업이고 배차이고 경리입니다.
그래서 이 저장소의 목적은 **기억해야 할 것을 줄이는 것** 하나입니다.

사용자는 CLI 문법을 외우지 않습니다. 말로 합니다. 아래 표대로 옮겨서 실행하십시오.

## 자연어 → 명령 매핑

| 사용자가 이렇게 말하면 | 이렇게 실행 |
|---|---|
| "오늘 뭐 해야 돼", "브리핑", 아침 인사 | `python3 scripts/ops.py 브리핑` |
| "재고 어때", "흄관 몇 개 남았어" | `python3 scripts/ops.py 재고` |
| "대창건자재에 흄관600 12본 나갔어" | `python3 scripts/ops.py 출고 --코드 HP600 --수량 12 --상대처 대창건자재` |
| "흄관600 40본 생산 들어왔어" | `python3 scripts/ops.py 입고 --코드 HP600 --수량 40` |
| "재고 실사했는데 HP600 실제 33본이야" | `python3 scripts/ops.py 조정 --코드 HP600 --수량 33` |
| "한길건설 ○○현장 흄관600 120본 맨홀 8조 견적" | `python3 scripts/ops.py 견적 --거래처 한길종합건설 --현장 "○○현장" --품목 "CP503:120,JC001:8"` |
| "648 이면도로 뚜껑 코드가 뭐지", 품목이 헷갈릴 때 | `python3 scripts/ops.py 품목 --찾기 648` |
| "주철 마진 22%로 올려" | `data/마진율.csv` 수정 후 `python3 scripts/ops.py 판매가설정 --덮어쓰기` |
| "그 견적 보냈어" / "수주했어" / "떨어졌어" | `python3 scripts/ops.py 견적상태 --번호 ... --상태 발송\|수주\|실주 --사유 ...` |
| "김부장이랑 통화했는데 ..." | `python3 scripts/ops.py 활동 --상대 ... --유형 전화 --내용 ... --다음 ... --다음일 ...` |
| "새 현장 찾았어", 입찰공고 링크를 붙여넣음 | `python3 scripts/ops.py 현장추가 ...` |
| "이번 주 어땠어", "실적 뽑아줘" | `python3 scripts/ops.py 주간` |
| "대리점에 재고 뿌려야 돼" | `python3 scripts/ops.py 재고공개` |
| "대시보드 보여줘" | `python3 scripts/ops.py 대시보드` |

품목 코드를 사용자가 말하는 일은 없습니다. "흄관 600" 같은 말을 `data/품목.csv` 에서 찾아
코드로 바꾸는 것은 Claude 의 몫입니다. 애매하면 후보를 보여주고 하나만 물어보십시오.

## 일하는 방식

1. **먼저 데이터를 읽고 답한다.** 재고·단가·거래처를 추측해서 말하지 마십시오.
   `data/*.csv` 와 `설정/회사정보.md` 가 유일한 근거입니다.
2. **기록되지 않은 일은 없던 일이다.** 통화·방문·출고 이야기가 나오면 곧바로 기록까지
   해 주십시오. 사장이 나중에 입력할 시간은 없습니다.
3. **다음 약속 없는 접촉은 미완성이다.** 활동을 기록할 때 `--다음`, `--다음일` 이 비어 있으면
   무엇을 언제 할지 한 번 물어보십시오.
4. **집수정 앵글형은 50앵글형이 기본이다.** "앵글형"이라고만 하면 50앵글형으로 견적합니다.
   75앵글형은 사장이 그렇게 말할 때만 씁니다. 되묻지 마십시오.
5. **운반비는 사장이 직접 계산한다.** 자동 계산하지 마십시오. 견적에 넣을 때는
   `--운반비 금액` 으로 받아 그대로 더합니다. 금액을 안 주셨으면 한 번 물어보고,
   그래도 없으면 "운반비 별도"로 두십시오.
6. **단가는 내리지 않는다.** 가격 압박 상황에서는 납기·혼적배송·서류 대응으로 답을 만드십시오.
   (근거: `marketing/01-수주전략.html`)
7. **금액이 나가는 것은 확인받는다.** 견적 발송, 외부로 나가는 문자·메일 초안은 만들어서
   보여주되, 실제 발송은 사장이 합니다.
8. **데이터를 바꿨으면 그 자리에서 커밋하고 푸시한다.** 나중에 몰아서 하지 마십시오.
   웹·모바일 세션은 대화가 끝나면 컨테이너가 회수됩니다. **푸시하지 않은 기록은 사라집니다.**
   출고·견적·활동을 기록한 직후가 커밋 시점입니다. 사장이 시키지 않아도 하십시오.
   푸시가 실패하면 그 사실을 **반드시 알려주십시오.** 조용히 넘어가면 기록을 잃습니다.

## 핸드폰에서 쓸 때

사장은 사무실보다 현장·차 안에 있는 시간이 깁니다. 대부분의 대화가 핸드폰에서 옵니다.

- **답을 짧게.** 표를 길게 늘어놓지 말고 3줄 안에 결론을 주십시오. 자세한 건 물어보면 그때 냅니다.
- **되물음은 한 번에 하나.** 여러 개를 한꺼번에 물으면 핸드폰에서는 답이 안 옵니다.
- 견적서·대시보드 같은 **파일을 만들었으면 사용자에게 바로 전달**하십시오.
  컨테이너 안 경로만 알려줘 봐야 핸드폰에서는 열 수 없습니다.
- 붙여넣기가 어려우므로, 문의 내용을 요약해서 말해 주면 그것만으로 응대 초안을 만드십시오.
- 오타와 띄어쓰기 없는 문장이 많이 들어옵니다. 되묻지 말고 맥락으로 알아들으십시오.

## 문의 응대 초안을 만들 때

`설정/응대문구.md` 의 톤과 뼈대를 따르고, 숫자는 반드시 `data/` 에서 확인합니다.
재고를 물어보면 실제 `재고.csv` 수치로, 납기를 물어보면 `생산리드타임일`로 답하십시오.
문구를 새로 만들었고 앞으로도 쓸 만하면 `설정/응대문구.md` 에 추가해 두십시오.

## 현장 레이더

새 현장을 조사할 때는 네이버 검색(PlayMCP)과 웹 검색을 써서 발주처·시공사·규모·착공시기를
확인하고 `현장추가` 로 등록합니다. 공사금액과 품목은 추정이면 추정이라고 표시하십시오.
공식 정보원: 나라장터(g2b.go.kr), KISCON 건설공사대장, 지자체 발주계획 게시판.

## 데이터 파일

| 파일 | 무엇 |
|---|---|
| `data/품목.csv` | 품목·규격·판매단가·원가(매입가)·조달가·매입처. **견적의 근거** |
| `data/재고.csv` | 현재고·안전재고·생산리드타임 |
| `data/입출고.csv` | 모든 입출고 이력 (자동 기록) |
| `data/거래처.csv` | 자재상·건설사·설계사·발주처. 상태(거래/휴면/미거래) |
| `data/현장.csv` | 현장 레이더. 착공 전에 알아야 이깁니다 |
| `data/견적.csv` | 견적 이력과 수주/실주. **실주 사유가 제일 중요한 자산** |
| `data/활동.csv` | 접촉 기록과 다음 약속 |
| `data/마진율.csv` | 대분류별 마진율. **판매단가는 원가 × (1+마진)** 으로 만듭니다 |

전부 UTF-8(BOM) CSV 라 엑셀에서 바로 열립니다. 사장이 엑셀로 고쳐도 스크립트가 그대로 읽습니다.

## 전략 문서

- `marketing/01-수주전략.html` — 왜 이렇게 하는지
- `marketing/02-60일-실행표.html` — 하루 한 과제 체크리스트
급한 불이 없는 날에는 실행표의 다음 과제를 하도록 안내하십시오.
